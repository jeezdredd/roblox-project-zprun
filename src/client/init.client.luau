--!strict

local ReplicatedFirst = game:GetService("ReplicatedFirst")

local BootState = require(ReplicatedFirst:WaitForChild("BootState"))
local BootConfig = require(ReplicatedFirst:WaitForChild("BootConfig"))

local FlowController = require(script.controllers.FlowController)
local SquadController = require(script.controllers.SquadController)
local DeathController = require(script.controllers.DeathController)
local SettingsService = require(script.systems.SettingsService)
local MusicController = require(script.systems.MusicController)
local FpsCounter = require(script.systems.FpsCounter)
local SettingsApply = require(script.systems.SettingsApply)
local GoreController = require(script.systems.GoreController)
local DefaultSoundMuter = require(script.systems.DefaultSoundMuter)
local LightingDirector = require(script.systems.LightingDirector)
local CityAmbience = require(script.systems.CityAmbience)
local ZombieAudio = require(script.systems.ZombieAudio)
local FlashlightController = require(script.systems.FlashlightController)
local FirstPersonController = require(script.controllers.FirstPersonController)
local ExertionState = require(script.controllers.ExertionState)
local BodyMotionController = require(script.controllers.BodyMotionController)
local FootPlanting = require(script.controllers.FootPlanting)
local CameraEffectsController = require(script.controllers.CameraEffectsController)
local MotionVfx = require(script.systems.MotionVfx)
local WorldVfx = require(script.systems.WorldVfx)
local PostFx = require(script.systems.PostFx)
local PlayerSfx = require(script.systems.PlayerSfx)
local WorldSfx = require(script.systems.WorldSfx)
local UiSfx = require(script.systems.UiSfx)
local ZombieVfx = require(script.systems.ZombieVfx)
local AnimationController = require(script.controllers.AnimationController)
local FootstepController = require(script.controllers.FootstepController)
local BreathingController = require(script.controllers.BreathingController)
local HudGui = require(script.ui.HudGui)
local RunHud = require(script.ui.RunHud)
local GoreWarningGui = require(script.ui.GoreWarningGui)

BootState.awaitReady(BootConfig.READY_TIMEOUT)

SettingsService.init()
MusicController.init()
FpsCounter.init()
SettingsApply.init()
GoreController.init()
DefaultSoundMuter.init()
LightingDirector.init()
CityAmbience.init()
ZombieAudio.init()
FlashlightController.init()
FirstPersonController.init()

ExertionState.init()
BodyMotionController.init()
FootPlanting.init()
CameraEffectsController.init()
MotionVfx.init()
WorldVfx.init()
PostFx.init()
PlayerSfx.init()
WorldSfx.init()
UiSfx.init()
ZombieVfx.init()
AnimationController.init()
FootstepController.init()
BreathingController.init()

FlowController.init()
SquadController.init()
DeathController.init()

HudGui.init()
RunHud.init()
GoreWarningGui.showOnce()

print("[TaskForceZ][Client] Client started")
