--!strict

local GameConstants = {
	BASE_RUN_SPEED = 24,
	MAX_RUN_SPEED = 60,
	HUB_WALK_SPEED = 16,
	HUB_JUMP_HEIGHT = 7.2,
	SPEED_GAIN_PER_STUD = 0.002,
	DEFAULT_DIFFICULTY = "Medium",

	ZOMBIE_AGGRO_DISTANCE_THRESHOLD = 5000,
	ZOMBIE_AGGRO_RANGE = 120,
	ZOMBIE_AI_TICK = 0.2,
	ZOMBIE_SPAWN_EDGE_MARGIN = 4,
	ZOMBIE_CORPSE_LIFETIME = 2,
	LANE_WIDTH = 240,
	APRON_WIDTH = 110,

	RUN_DIRECTION = Vector3.new(0, 0, -1),
	LATERAL_SPEED_WEIGHT = 0.7,
	WALL_WRAP_MARGIN = 2,

	START_PLATFORM_LENGTH = 64,
	CHUNK_LENGTH = 128,
	TRANSITION_CHUNKS = 2,
	SPAWN_AHEAD_DISTANCE = 768,
	DESPAWN_BEHIND_DISTANCE = 160,

	BARRIER_HEIGHT = 60,
	BARRIER_THICKNESS = 2,
	WALL_COLOR = Color3.fromRGB(170, 60, 60),
	WALL_TRANSPARENCY = 1,

	FIELD_OBSTACLE_SCALE = 3.2,
	FIELD_CLUSTER_CHANCE = 0.55,

	CAMERA_BACK_DISTANCE = 16,
	CAMERA_HEIGHT = 11,
	CAMERA_LOOK_AHEAD = 20,
	CAMERA_SMOOTHING = 0.15,
}

return table.freeze(GameConstants)
